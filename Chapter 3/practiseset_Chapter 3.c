#include<stdio.h>

int main() {
// 1. What will be the output of this program 
    // int a = 10; 
    //     if (a==11) //Answer is I am 11
    //     printf("I am 11"); 
    //     else  
    //     printf("I am not 11"); 
    // return 0;

/*2. Write a program to determine whether a student has passed or failed. To pass, a 
student requires a total of 40% and at least 33% in each subject. Assume there 
are three subjects and take the marks as input from the user.*/

    // int m1,m2,m3;
    // printf(" Enter m1\n");
    // scanf("%d", &m1);
    // printf(" Enter m2\n");
    // scanf("%d", &m2);
    // printf(" Enter m3\n");
    // scanf("%d", &m3);
    // printf(" The marks are %d %d and %d\n", m1,m2,m3);

    // if(m1<33 || m2<33 || m3<33){
    //     printf(" You are failed ");
    // }
    // else if((m1+m2+m3)/3<40){
    //     printf(" You are failed due to less percentage \n");
    // }
    // else{
    //     printf(" You are passed ");
    // }
//3. Calculate income tax paid by an employee to the government as per the slabs 
            // mentioned below: 
            //  Income Slab                       
            //          2.5 – 5.0L                               
            //          5.0L - 10.0L                          
            //          Above 10.0L                         
            //      Tax  
            //      5% 
            //      20% 
            //      30%

            // int inc; float tax = 0.0;
            // printf(" Enter income \n");
            // scanf("%d", &inc);
            // if(inc<=250000)
            // {
            //     tax = 0;
            // }
            // else if(inc<=500000){
            //     tax = 0.05 * (inc - 250000);
            // }
            //  else if(inc<=1000000){
            //     tax = 0.05 * ( 500000 - 250000) + 0.2 * (inc - 500000);
            // }
            //  else {
            //    tax = 0.05 * ( 500000 - 250000) + 0.2 * (1000000 - 500000) + 0.3 * (inc - 1000000);
            // }
            // printf(" Total tax need to pay is %.2f", tax);

// 4. Write a program to find whether a year entered by the user is a leap year or not. 
// Take year as an input from the user. 


            // int y;
            // printf(" Enter year \n");
            // scanf("%d", &y);

            // if((y%4==0 && y%100!=0) || y%400==0){
            //     printf("This is a leap year");
            // }
            // else{
            //     printf("This is not a leap year");
            // }
// 5. Write a program to determine whether a character entered by the user is 
//lowercase or not. 

        // char ch;
        // printf(" Enter a character\n");
        // scanf("%c", &ch);
        // if(ch>=97 && ch<=122){
        //     printf(" This charcter is lowercase\n");
        // }
        // else{
        //     printf(" This charcter is not lowercase\n");
        // }
        // printf("The value of charcter is %d", ch);

// 6. Write a program to find greatest of four numbers entered by the user.

        // int a = 4 ,b = 6 ,c = 118 ,d = 10 ;
        // if(a>b && a>c && a>d){
        //     printf(" The greatest of all is %d", a);
        // }
        // if(b>a && b>c && b>d){
        //     printf(" The greatest of all is %d", b);
        // }
        // if(c>a && c>b && c>d){
        //     printf(" The greatest of all is %d", c);
        // }
        // if(d>a && d>b && d>c){
        //     printf(" The greatest of all is %d", d);
        // }
 }

