#include<stdio.h>

int main() {
    if(0){
        printf("This if is executed\n");//will not execute
    }
      if(2334){
        printf("This if is also executed");//will execute
    }

    return 0;
}