#include<stdio.h>

int main() {
    int a = 6;
    int b = 3;
    int c = a + b;
    printf("The value of a is %d and value of b is %d and the sum is %d\n", a,b,c);

    printf("The reaminder is %d\n", a%b);

    //int d = a^b; not for exponentiation
    

    return 0;
}
